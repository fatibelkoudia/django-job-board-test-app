services:
  web:
    image: django-job-board:latest
    env_file:
      - .envlocal
    ports:
      - "8000:8000"
    depends_on:
      - db

  db:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: p4ssword123!
      POSTGRES_DB: mydatabase
    ports:
      - "5432:5432"
